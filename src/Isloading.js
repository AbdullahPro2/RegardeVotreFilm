function Isloading() {
  const loader = {
    display: "flex",
    justifyContent: "center",
    alignItem: "center",
    fontSize: "3rem",
    marginTop: "3rem",
  };
  return <div style={loader}>Loading...</div>;
}

export default Isloading;
